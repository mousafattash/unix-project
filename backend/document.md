# Dictionary Application
## Setup Instructions
1. Install LAMP stack.
2. Create a MySQL database named `dictionary`.
3. Use the provided `index.php` file to query the database.

## Features
- Input a word and get its meaning.
- Database connection via PHP.